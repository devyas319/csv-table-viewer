name: Publish Extension

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      publish_target:
        description: 'Publish target'
        required: true
        default: 'both'
        type: choice
        options:
          - both
          - vsce
          - ovsx

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Compile
        run: npm run compile

      - name: Package Open VSX extension
        run: npx @vscode/vsce package -o ovsx-csv-table-viewer.vsix

      - name: Rename for VS Code Marketplace
        run: npm pkg set name=devyas319-csv-table-viewer

      - name: Package VS Code extension
        run: npx @vscode/vsce package -o vsce-csv-table-viewer.vsix

      - name: Upload OVSX artifact
        uses: actions/upload-artifact@v4
        with:
          name: ovsx-vsix
          path: ovsx-csv-table-viewer.vsix

      - name: Upload VSCE artifact
        uses: actions/upload-artifact@v4
        with:
          name: vsce-vsix
          path: vsce-csv-table-viewer.vsix

  publish-vsce:
    needs: build
    runs-on: ubuntu-latest
    if: >
      github.event_name == 'release' ||
      github.event.inputs.publish_target == 'both' ||
      github.event.inputs.publish_target == 'vsce'
    steps:
      - name: Download VSCE VSIX
        uses: actions/download-artifact@v4
        with:
          name: vsce-vsix

      - name: Publish to VS Code Marketplace
        run: npx @vscode/vsce publish --packagePath vsce-csv-table-viewer.vsix
        env:
          VSCE_PAT: ${{ secrets.VSCE_PAT }}

  publish-ovsx:
    needs: build
    runs-on: ubuntu-latest
    if: >
      github.event_name == 'release' ||
      github.event.inputs.publish_target == 'both' ||
      github.event.inputs.publish_target == 'ovsx'
    steps:
      - name: Download OVSX VSIX
        uses: actions/download-artifact@v4
        with:
          name: ovsx-vsix

      - name: Publish to Open VSX
        run: npx ovsx publish ovsx-csv-table-viewer.vsix -p ${{ secrets.OVSX_PAT }}
